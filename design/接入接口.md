做一个接口，用于控制玩家的登入流程和状态，它需要把大多数API都进行封装，转变为一个HyperZonePlayer对象

接口放在API中，实现位于 velocity 模块下
有至少以下几个主要函数
- canRegister - 用于判断是否能够进行注册，主要依据是是否有数据库内的Profile
- isVerified - 用于判断是否验证过，这使得LimboAuthSessionOverVerify接口没用了
- overVerify - 结束玩家的验证
- sendMessage(message: Component) - 用于发送消息

不知道怎么实现就先把接口和代码预留出来